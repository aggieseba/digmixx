
<%= search_form_for @search, url: search_index_path, :action => 'index'  do |f| %>
  <%= f.condition_fields do |c| %>
    <%= render "condition_fields", f: c%>
  <% end %>
  <p><%= link_to_add_fields "Add Conditions", f, :condition %>
  <div class="actions"><%= f.submit "Search" %></div>
<% end %>

<%= search_form_for @search, url: search_index_path, :action => 'index' do |f| %>
  <%= f.label :tags_name_cont, 'タグ' %>
  <%= f.text_field :tags_name_cont %>
  <div>
    <%= f.submit '検索する' %>
  </div>
<% end %>

<tr>
  <th><%= sort_link(@search, :voice, "voice") %></th>
  <th><%= sort_link(@search, :length, "length") %></th>
</tr>

<%= params[:q] %>
<%= params[:tag] %>

<% @mixes.each do |mix| %><%# もしカテゴリーがなかった時は無視する構文を書く %>
          <article class="hentry" role="article">
            <header class="article-header">
              <p class="update-time"><%= mix.updated_at.strftime("%x") %></p>
            </header>
            <div class="entry-content">
              <%= raw mix.content %>
            </div>
            <div class="entry-meta">
              <div class="entry-category">
                <ul>
                  <% mix.tag_list.each do |tag| %>
                    <a href="<%= tag %>"><li class="category-btn"><%= tag %></li></a>
                  <% end %>
                </ul>
              </div>
              <% if false %>
              <div class="entry-playlist">
                <%= mix.artistname %>
              </div>
              <% end %>
            </div>
          </article>
          <% end %>


